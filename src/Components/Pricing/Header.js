import React from 'react'

const Header = () => {
    return (
        <main className="head d-flex justify-content-center align-items-center">
            <h1 className="text-center text-uppercase pricing-plan">Pricing plans</h1>
        </main>
    );
};

export default Header;